# TODO

Running list of work items for DailyDefense. Move items into the changelog
(`docs/CHANGELOG-vN.md`) as they ship.

## Open

- [ ] **Update the user interface to use Material Design.** Refresh the HUD,
      sidebar, overlay, and buttons to follow Material Design conventions
      (typography, elevation/shadows, ripple-style press feedback, colour
      tokens). Keep the iOS-friendly touch behaviour from v0.4.1.

## Done

_(empty — move completed items here with the version they shipped in,
e.g. `- [x] Thing — v0.5.0`)_
